document.addEventListener("DOMContentLoaded", () => {
    const PageSize = document.getElementById("pageSize");
    const PageHeight = window.innerHeight;
    const PageWidth = window.innerWidth;

    PageSize.style.height = `${PageHeight}px`;
    
});
